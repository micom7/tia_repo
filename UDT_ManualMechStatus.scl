// File: UDT_ManualMechStatus.scl
// Статус механізму для SCADA UI (тільки діагностика)
TYPE "UDT_ManualMechStatus" :
STRUCT
    ManualAllowed : BOOL;      // TRUE = SCADA може керувати
    CurrentOwner  : USINT;     // поточний власник
    CurrentStatus : UINT;      // поточний статус механізму
END_STRUCT;
END_TYPE